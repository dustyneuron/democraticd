#!/usr/bin/env python3.3

def build(package_name):
    # apt-get source package_name (gets old version)
    
    # This works cos auto* downloads the latest upstreams from github/roboji/*
    # dpkg-buildpackage
    
    # copy new deb version to the repository
    
    # apt-get update && apt-get upgrade package_name
    
